# Repository context

Read and respect repository instructions such as `AGENTS.md`, `CONTEXT.md`, `CONTRIBUTING.md`, and `docs/adr/*.md` when they exist. If project docs and implementation differ, surface the contradiction instead of silently choosing one.
